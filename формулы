a1=orbit.periapsis_altitude+orbit.body.equatorial_radius
a2=orbit.apoapsis_altitude+orbit.body.equatorial_radius
G=space_center.g
Mcb=orbit.body.mass
скорость в апоапсисе math.sqrt(  (2*G*M*a1)/(a2*(a1 + a2)) )
скорость в переапсисе math.sqrt( (2*G*M*a2)/(a1*(a1 + a2)) )
большая полуось (a1 + a2)/2
период обращения math.sqrt( (4*Pi**2*a2**3)/(Mcb*G) )


    #S = P * Err + (1/Ti)*Int + Td*dErr 

    #S — Угол, который следует выдать;
    #P — пропорциональный коэффициент (обратите внимание — вынесен за скобки, почему — чуть ниже опишу);
    #Ti — постоянная времени интегрирования. Обратите внимание — в расчетах используется обратная величина;
    #Td — постоянная времени дифференцирования
    #Err — текущее рассогласование (разница между уставкой и измеренной скоростью)
    #dErr — производная рассогласования (разница между текущей и прошлой ошибкой)
    #Int — накопленный интеграл рассогласования (сумма всех Err'ов, кои мы видели)

